# 第三十七轮：共享年度模型上的受约束状态截距修正

使用 R36 年度四状态，年度神经网络、变换和原分类系数保持不变。只用季度新增成熟成员拟合小幅状态截距；新增状态计数不足 20 则回退年度。预先固定 ridge=20、|logit 修正|≤0.5。比较分状态主方案、同样本同门控同总惩罚的统一修正，以及状态修正减半。

冻结后运行：`research_v4/.venv_gpu/Scripts/python.exe -B -u research_v37/run37.py prepare contract fit score evaluate verify report`。图像审查和结果解读复核后单独运行 `delivery37.py`。

全部 272 周历史已经查看，不是新盲测；不以结果选择强度或启用状态。原 R25 近期 73/125、58.4% 单独保留，本轮直接年度 R25 对照为 71/125、56.8%。
