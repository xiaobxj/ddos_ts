# 第 27 轮：训练窗口与种子融合诊断

主实验固定神经网络更新次数，比较最近 5 年、最近 3 年训练窗口与第 26 轮全历史对照。短窗口重复抽取保留样本，因此不是自然 20 遍训练。所有输入处理和分类拟合都使用各窗口唯一的成熟训练样本。保留 R18/R19/R23/R25、原 MSE 和匹配窗口的训练频率对照。

36 次新神经训练、144 次分类拟合全部完成后才评分。三个种子的平均概率规则不变；种子分歧及过去校准预测的可用性只作诊断，本轮不拟合校准器。

用已有 GPU Python 执行 `run27.py prepare contract train score evaluate verify report`。报告阶段自动使用已安装 matplotlib 的默认 Python 3.14，不安装软件。检查图表后单独执行 `delivery27.py`；不要通过仍在写入的日志包装器执行交付冻结。

结果位于 `results/第二十七轮测试报告.md`。保持所有先前 3,813 份证据不变；第 26 轮的预检查归档和报告环境修复亦保留。历史已被多次查看，本轮不声称独立确认或交易盈利。

本轮已完成。冻结版 `verify27.py` 在末尾保存计时记录时存在变量覆盖错误，最终通过独立适配器更名该局部变量后完整重跑验证；结果与首次输出逐字节相同。参见 `results/执行记录补充.md`、`results/verification_metadata_repair.json` 及适配器源码副本。模型、评分、统计和全部断言保持不变。
