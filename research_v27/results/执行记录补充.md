# 第 27 轮执行记录补充

原冻结协议、训练、评分和统计源码均保持不变。36 次新神经训练、144 次分类拟合完成后统一评分，未因结果增加或删除候选。

首次复核已经完成 36 个检查点、144 个分类解、预测、指标和种子诊断检查；在最后保存复核 JSON 时，校准日期边界变量 `start` 覆盖了最初的计时变量，导致 `elapsed_seconds` 的浮点减字符串运算报错。原日志和已生成的复核表保存在 `verification_attempt1`，并附哈希清单。

使用单独适配器，仅把校准日期边界的局部变量更名为 `calibration_start`，完整重跑全部复核。原 `verify27.py` 文件保持冻结，没有改变断言、容差、模型、预测或统计检验。适配器代码副本为 `verification_metadata_adapter.txt`，原源码哈希、执行正文哈希及修复结果记录在 `verification_metadata_repair.json`。

第二次复核通过，独立求解和检查点回放表与首次输出逐字节相同。计入首次尝试，实际进行了 72 次检查点回放、288 次独立求解；它们是 36 个不同检查点、144 个不同分类解的重复验证。神经训练和主分类拟合没有重跑。

报告完成视觉与文字检查时，将“以前年年底”澄清为“以上一年年底为训练截止日”。这是文字修正，表格、模型实际截止日期和指标均未变化。原报告及原报告清单保存在 `report_editorial_archive`。
