第四十三轮检查 R42 月度决定前实际保存的旧修正，在当前成熟验证窗口、原通过后才成熟的子集、上月底后才成熟的子集上，与当月新候选和年度基线对照。

所有 1,088 个决定均保留；不恢复已清空的旧记录。到期记录单列，主要诊断关注尚未到期记录。证据不足不记为通过。原三种子、四状态、52／13 周和成熟隔离规则不变。本轮没有新训练、新路由策略、新准确率历史或新 p 值。

先冻结 8 个源文件、协议、输入和 5,754 个旧文件；独立检查原记录和样本成熟时间，冻结检查对象；计算诊断；独立重算并测试未来数据隔离；生成报告与图。图表和解读核对后交付。

运行：`research_v4/.venv_gpu/Scripts/python.exe -B -u research_v43/run43.py prepare contract diagnose verify report`。最后单独运行 `delivery43.py`，以免交付清单生成后日志再次变化。
