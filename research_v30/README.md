# 成熟预测误差触发模型更新

固定 5 年窗口、自然 20 遍模型，统一监测 R19 最近两个各 8 周的成熟预测是否都落后于其当时的频率基准。季末触发、年末固定更新，换模型后重新积累评估记录。

复用 R28/R29 的冻结模型及旧预测，本轮不新增神经训练或分类拟合。先冻结全部可能的同年度预测覆盖，补充推理，再严格逐期回放；未来标签及未被实际使用的模型预测不参与当前触发。

运行：`research_v4/.venv_gpu/Scripts/python.exe -B -u research_v30/run30.py prepare contract extend score evaluate verify report`。

报告单独使用已有 Python 3.14 绘图库。完成视觉复核后单独运行 `delivery30.py`，以免对仍在写入的日志做交付哈希。保留全部旧证据和历史结果；本轮不是新盲测。
