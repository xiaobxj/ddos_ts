# 第四十二轮：验证支持不足时有限期保留修正

在 R40 月度验证方案上，只改变支持不足时的行为。保存正式通过的各状态原参数，最多使用至正式通过日后的下一个季度末。仅验证样本不足且训练支持仍足够时保留；保留不续期；其他拒绝清空记录且不能复活；年度和第一季度回退不变。

复用已冻结的候选参数与门控，无新增神经或标量训练。先冻结 10 个源文件、协议、输入及 5,691 个旧文件，形成全部保留决定后再评分。运行 `research_v4/.venv_gpu/Scripts/python.exe -B -u research_v42/run42.py prepare contract route score evaluate verify report`。图表和解读检查后运行 `delivery42.py`。

一条新预测历史，36 项预定探索性比较，原 22 条预测历史保持不变。全部历史此前已查看，不是新盲测。
