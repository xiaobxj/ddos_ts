# 第三十九轮：成熟周样本训练修正，再由保留验证决定启用

保持自然 20 遍年度模型与交互项。每非年末季度用较早 52 个成熟周样本训练受约束截距，随后 13 个成熟周信号作验证；训练标签必须在最早验证信号前已经成熟。状态修正要求状态训练至少 10 周、状态验证至少 5 周，Brier 改善且正确周数不减少才启用。验证后不重拟合。配套统一截距及两种相同参数的无门控对照。早期不足和每年 Q1 回退年度。

运行：`research_v4/.venv_gpu/Scripts/python.exe -B -u research_v39/run39.py prepare contract fit validate score evaluate verify report`。11 个源文件、协议与 5,484 个旧文件先冻结。图像和结果解读复核后运行 `delivery39.py`。

所有历史已查看，这是新校准规则的历史探索，不是新盲测，也没有新增神经网络训练或部署。
