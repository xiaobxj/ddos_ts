第51轮把既有采集、季度／年度参数更新、当周预测、标签结算和固定评估接成一个手动入口。五套方案、市场与订单交互、原三个种子、训练窗口和第49轮前瞻比较安排均保持原样。

在 `D:\ddos_v3` 查看当前计划：

```powershell
& .\research_v4\.venv_gpu\Scripts\python.exe -B -u research_v51\run51.py plan
```

在工作日北京时间18点之后执行一次完整流程：

```powershell
& .\research_v4\.venv_gpu\Scripts\python.exe -B -u research_v51\run51.py cycle
```

也可使用短入口 `& .\research_v51\run.ps1 cycle`。两种入口都只运行一次；没有安装计划任务或后台服务。18点前运行只检查状态和保存运行日志，不采集、不更新参数、不提交预测或标签。

首个候选日是 **2026-09-18**，应在当天完整日线可用后、北京时间 **18:00至23:00之前**运行 `cycle`，给推理和写入留出时间。当天快照缺失会显示 `DATA_PENDING`，当日窗口内可再次运行。不能用前一交易日代替周五，也不能过期补记。实际到账行情决定可用性，这个入口没有自行推断交易所节假日。

同一命令的执行顺序如下：

1. 验证冻结文件、参数注册凭据和账本；若当天快照尚未到齐，采集并存档。
2. 检查新快照是否完整保留之前已经观察到的行情前缀，修订或缺行会停止后续步骤。
3. 按时间顺序更新已到截止点、已有覆盖数据且尚未注册的参数。2026-09-30更新下一季度；2026-12-31委托原第50轮流程做年度重训。不会提前拟合未来截止点。
4. 再次检查时钟，只在当前候选周五18点至23点之间提交一条预测。已提交则跳过；采集或重训耗时跨过截止时间也不会迟写。
5. 对已记录的预测检查原联合成熟条件，成熟后追加标签，再按第49轮规则汇总。主要评估一旦保存便直接复用。

原快照、预测、标签、参数注册仍写在 `prospective_r49`；未来参数构建仍写在 `prospective_r50/builds`；本轮运行日志单独写在 `prospective_r51/runs`。每次运行有一个 `*.started.json` 开始凭据，以及完成时的同名 `*.json` 结果。`FAILED` 会保留错误、已完成步骤和此前成功提交的证据，进程退出码为1；`DATA_PENDING` 表示还缺数据，可以在合适时点重试。

完整流程使用 `prospective_r51/cycle.lock` 防止两个新入口同时执行；原账本也继续使用逐条写锁。运行新入口期间不要并行执行旧入口的写入命令。若进程被强制终止，可能留下开始凭据和锁，应先检查对应进程与账本再处理；程序不会自动删除其他进程的锁或回滚已提交事件。

工程检查使用单独标记的模拟日期和模拟后续行情，调用真实原模型及记录／结算函数，不能作为准确率、收益或前瞻成绩。模拟夹具保存在验证报告指向的 `_work/r51_SYNTHETIC_TEST_ONLY_*` 目录，完全独立于真实账本。生产命令没有日期覆盖、测试行情或自选账本参数。

当前仍需等待真实未来样本。中途只作描述性汇总；主要评估最早2027-10-01，且至少40个完整成熟的共同样本、没有待成熟的已提交预测。之前的局部改善和候选都保留，但不会凭工程模拟重新排名或选优。

[本轮结果](results/结果与运行安排.md) · [实现协议](protocol.json) · [22项工程验证](results/verification.json) · [模拟链路明细](results/synthetic_rehearsal.json)
