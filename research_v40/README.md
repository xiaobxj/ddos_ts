# 第四十轮：同一周样本状态修正的月度与季度更新

沿用 R39 的自然 20 遍年度模型、已有交互项、状态边界、成熟周样本、52／13 训练验证隔离、训练 10／验证 5 的状态支持门槛、正则 20、截距上限 0.5、Brier 改善且正确数不减少的验证规则。本轮仅将修正训练与验证的联合更新频率从季度改为月度。

新增月度验证与同参数直接使用两个候选。每年第一季度仍回退年度预测；月末周五信号只能使用更早的月底决定。所有季度重合点的样本、参数与门控必须等于 R39。所有历史已查看；不是盲测，没有新增神经训练、收益或部署测试。

运行 `research_v4/.venv_gpu/Scripts/python.exe -B -u research_v40/run40.py prepare contract fit validate score evaluate verify report`。13 个源文件、协议、输入与 5,554 个旧证据文件先冻结。查看图表与结果解读后运行 `delivery40.py`。
