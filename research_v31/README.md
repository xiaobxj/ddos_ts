# 新旧模型并行试运行与固定融合

第 31 轮固定两个候选：季度候选模型先试运行，再凭共同成熟周上的 Brier 和方向正确数决定是否接替；以及年度与季度模型各占一半的固定融合。保留年度、季度、状态触发、误差触发和旧训练量年度方案。

全部神经网络、分类头、原始预测复用冻结证据；不新增训练或推理。季末新训练的模型只能进入下一轮试运行，不能用训练内回算冒充过去实发预测。所有历史结果此前已被查看，本轮不是新盲测。

运行：`research_v4/.venv_gpu/Scripts/python.exe -B -u research_v31/run31.py prepare contract score evaluate verify report`。

完成图像视觉检查后，单独运行 `delivery31.py`，避免对仍在写入的日志计算最终交付哈希。
